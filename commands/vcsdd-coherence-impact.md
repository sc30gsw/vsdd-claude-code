# /vcsdd-coherence-impact

Run BFS forward-impact analysis from changed nodes through the CEG.
Also surfaces CoDD `must_review` convention alerts alongside the usual
Green / Amber / Gray downstream impact bands.

**Usage:** `/vcsdd-coherence-impact [node_id] [--diff <target>]`

Follows the instructions in the `vcsdd-coherence-impact` skill.
